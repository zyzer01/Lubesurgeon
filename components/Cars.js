import React from 'react';
import Heading from './utilities/Heading';
import Flexitems from './utilities/Flexitems';

function Cars(props) {
  return (
    <div>
      <section>
        <Heading headTitle="Cars" sub="We service nearly all makes and models." />
      </section>
    </div>
  );
}

export default Cars;
